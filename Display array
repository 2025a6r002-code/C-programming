//WAP to display all the elements of an array.
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int as[n];
    for( int i=0; i<n; i++){
    scanf("%d",&as[i]);}
    for(int j=0; j<n ; j++){
        printf("%d ",as[j]);
    }
        
    return 0;
}
