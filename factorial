#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
   int N;
    scanf("%d",&N);
    
    int fac=1;
    for(int i=1;i<=N;i++){
        fac=fac*i;
    }
    printf("%d",fac);
    return 0;
}
